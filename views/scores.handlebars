<div class="row">
  <div class="col-auto">
    <h2>Welcome, {{username}}!</h2>
  </div>
</div>

<div class="row mt-4">
  <div class="col-md-6">
   <h1>High Scores</h1>
    <ul>
        {{#each highScores}}
            <li>{{name}} - {{score}}</li>
        {{/each}}
    </ul>

</div>

<script src="./js/scores.js"></script>